{
  "$schema": "https://turborepo.com/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**"],
      "env": ["PREVIEW_SECRET", "PAYLOAD_SECRET", "DATABASE_URI", "NEXTAUTH_SECRET", "GOOGLE_CLIENT_ID", "GOOGLE_CLIENT_SECRET","BACKEND_URL","ENCRYPTION_SECRET", "REVALIDATE_SECRET", "GOOGLE_GEMINI_API", "CSRF_SECRET"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "check-types": {
      "dependsOn": ["^check-types"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "test": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "env": ["NODE_ENV", "DATABASE_URI", "PAYLOAD_SECRET", "UPLOADTHING_SECRET", "UPLOADTHING_APP_ID"]
    }
  }
}
