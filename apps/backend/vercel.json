{
  "version": 3,
  "builds": [
    {
      "src": "apps/backend/package.json",
      "use": "@vercel/node",
      "config": { "outputDirectory": "apps/backend/dist" }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "apps/backend/dist/$1"
    }
  ]
}
